# ==========================================
# STAGING / PREPRO
# ==========================================
# Este archivo se usa en Netlify para el branch 'staging'

# App URL (Netlify subdomain)
VITE_APP_URL=https://hackeed-staging.netlify.app

# DESHABILITAR rutas de debug en staging
VITE_DEBUG_MODE=false

# Moneda y país por defecto
DEFAULT_CURRENCY=EUR
DEFAULT_COUNTRY=ES

# NOTA: Las claves de Stripe, DB, etc. se configuran
# directamente en Netlify UI como variables de entorno
# para mayor seguridad
